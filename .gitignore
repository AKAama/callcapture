# Python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.coverage
.ruff_cache/
.pytest_cache/

# Swift
.build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Built app binary (Info.plist / entitlements are kept as source)
CallCapture.app/Contents/MacOS/

# macOS
.DS_Store

# Models (downloaded at runtime)
models/

# IDE
.idea/
.vscode/

# Agent / local session logs
.claude/
.worktrees/

# Local data (never commit recordings or the session DB)
*.wav
*.sqlite
*.db
*.db-wal
*.db-shm

# Built distribution artifacts
CallCapture-*.zip
*.dmg

# Env
.env
.env.local

# Code-signing secrets — NEVER commit (public repo). The whole folder is
# ignored, plus key material by extension as a backstop.
/signing/
*.p12
*.p8
*.cer
*.key
p12_password.txt
